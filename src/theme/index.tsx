import { createTheme } from '@mui/material/styles';

export const MyTheme = createTheme({
        palette: {
          primary: {
            main: '#000000',
          },
          secondary: {
            main: '#EB5757',
          },
        },
});